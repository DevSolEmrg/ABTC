const state = {
    patients: ['dd']
};

const getters = {
    patients: state => state.patients,
};

const actions = {
};

const mutations = {
};

export default {
    state,
    getters,
    actions,
    mutations
};
